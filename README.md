# Luis Ospina

A small personal website built with Next.js and TypeScript.

## Run locally

```bash
pnpm dev
```

Open http://localhost:3000 in your browser.

## Useful commands

- `pnpm lint` checks the code.
- `pnpm build` creates a production build.
